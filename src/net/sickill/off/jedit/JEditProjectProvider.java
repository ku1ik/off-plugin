/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package net.sickill.off.jedit;

import java.util.Collection;
import net.sickill.off.*;

/**
 *
 * @author kill
 */
public class JEditProjectProvider implements ProjectProvider {

    public Collection<ProjectFile> getProjectFiles() {
        throw new UnsupportedOperationException("Not supported yet.");
    }

    public String getProjectRootPath() {
        throw new UnsupportedOperationException("Not supported yet.");
    }

    public void setModel(OffListModel aThis) {
        throw new UnsupportedOperationException("Not supported yet.");
    }

}
		//this.icon = icon;

		// VPTFile file = e.getFile();
		/*
		 * String t = "<html>"+e.getHighlightedText()+" "; t += isSelected ? "" : "<font
		 * color=\"808080\">"; t += "["+file.getNodePath()+"]"; t += isSelected ? "" : "</font>";
		 * t += "</html>"; ((JLabel)comp).setText(t);
		 */
		// file.ge
		// String projectPath = file.getNodePath().substring(10);
		// ((JLabel)comp).setText(file.getName() + " [" +projectPath + "]");
		// ((JLabel)comp).setIcon(file.getIcon(false));
